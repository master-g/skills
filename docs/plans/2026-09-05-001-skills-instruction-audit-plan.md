---
title: "refactor: 收窄技能触发与执行边界"
type: refactor
status: complete
date: 2026-09-05
---

## 目标

落实本会话对全部 13 个技能和全局 AGENTS.md 的审计：减少误触发、无关前置任务和重复指令，保留授权、领域知识与可验证的完成条件。
依据为用户接受的审计及 https://x.com/pvncher/status/2095991462416490862 。文章是调整依据，不是模型效果已改善的证据。

## Checkpoint

- skills 原始 HEAD：`804e9f2ddcca066a0fc71091b98ac7bf8ffb9a1f`，原始工作区干净。
- 本地保留分支：`codex/checkpoint-skills-audit-20260905`。
- 实施分支：`codex/skills-instruction-audit`。
- 全局文件备份：`/tmp/skills-audit-checkpoint-20260905/`，`manifest.json` 记录三个文件的 SHA-256 与两个仓库的 HEAD。
- archdotfiles 原始 HEAD：`7ab534de711699aff7737ee3c4d0c6ff371539a0`。Claude、Pi 指令文件已有用户修改，按备份内容增量调整；保留网络搜索、语言风格和 Pi 插件管理块。其他脏文件不触碰。
- 恢复时查看 checkpoint 与当前 diff，只恢复本次命名路径；不要 reset 整个仓库或覆盖后来改动。

## 实施与语义复核阶段的非目标与不改范围

- 不调用其他模型，不跑跨模型效果实验，不安装依赖或技能。
- 不修改 HTML/CSS/JS 视觉实现、vendor、既有生成物、测试夹具内容或无关历史计划。
- 不同步已安装技能，不提交、推送、发布，也不修改 archdotfiles 其他配置和项目根 AGENTS.md。
- 不取消外部模型授权、不可逆操作口令、真实来源和验证证据要求。

## 实施顺序

1. **规则源**：精简全局 Codex 指令；同步 Claude/Pi 的共享工作边界，保留各自已有专属内容。bootstrap 取消主动初始化/回写触发，修正模板与脚本的无条件基线修复，按初始化/维护分流。
2. **路由与执行范围**：缩短描述，修正 Kami 依赖和 X 抓取路由；保存 Obsidian 条目不再附带全库 sweep；修正新 vault 的默认目录；makemake 只问实质缺口；保留 guide-me/wtf/farm 的手动调用策略和有效停止条件。
3. **按需读取**：长技能只拆真正可选的内容。show-me-html 保留输出契约，合成与验收按需引用；叙事 HTML 单独引用；book 去掉强制篇幅和数量，补实际预览验收；STORM 保留方法，明确大纲继续边界。弃用 effective-html 的入口改名为 ARCHIVE.md，保留历史资产，并更新唯一读取该文件的现有测试。
4. **验收与收尾**：核对元数据、引用、源模板一致性、checkpoint 后的完整 diff，更新本计划的实际结果。

## 验收标准

- 每个活动技能的 description 清楚表达能力与触发意图；已有显式调用策略不变。
- 无任务结束即初始化、无条件修复历史基线、全项目固定测试配额；普通阅读不自动转排版，单次入库不自动 sweep。
- 共享规则在三个全局文件一致，原有专属内容及用户已有修改保留；写入前比对 checkpoint 哈希，漂移则重新合并。
- bootstrap 新建和追加模式使用相同规则；在临时目录运行现有夹具，验证生成、幂等、校验及冲突拒绝，原夹具不变。
- 活动技能结构校验通过；校验器不支持的既有 frontmatter 字段单独说明，不为通过校验改变调用策略。
- effective-html 退出 SKILL.md 发现集合；入口改名对应的现有测试通过。完整旧套件与 checkpoint 对照不得新增失败；已有失败保留并报告。未运行安装器，不声称已安装副本已移除。
- 只改文档路由时不跑完整视觉回归；模板、构建器和 CSS 未变。archdotfiles 按项目要求运行 `make test`。
- 无断开的新增本地引用；`git diff --check` 通过。行为效果与模型耗时留作后续实际使用观察。

## 执行记录

- [x] Checkpoint 与实施分支已建立。
- [x] 实施计划已落盘。
- [x] 第一批：规则源。
- [x] 第二批：路由与范围。
- [x] 第三批：按需读取与归档。
- [x] 验收与实际结果。

## 实际结果

- 三个全局文件的共享工作方式、授权、验证与完成段落一致。保留 Claude 已有语言风格与搜索要求、Pi 的搜索及插件管理块。Codex 从 146 行精简为适用当前工具的规则；旧等待补丁留存在 checkpoint 备份，不继续加载。
- bootstrap 拆为初始化/维护两份按需文档；新建模板与脚本追加的两个规则块一致。已有项目内已生成的旧规则不被自动迁移。
- 普通阅读、单条入库不再带出排版或全库盘点；Kami 缺失时明确依赖，不自动安装、不冒称替代输出。新 vault 默认 inbox 的文档代码已在临时目录验证。
- book/STORM 保留方法与证据要求，去掉固定体量、代理数量及含混的重复审批；叙事格式/HTML、页面合成/验收改为按需引用。
- effective-html 仅将入口改名为 ARCHIVE.md，并调整读取该入口的一个现有测试。历史模板、视觉实现及 vendor 原样保留。
- 初次实施完成时，发现集合为 12 个活动技能；描述总字符数 `4846 → 518`，入口总行数 `2129 → 1211`。前值包含弃用技能；这些是文本统计，不是 token 或模型效果指标。

### 验证证据

- bootstrap：复制已有 node-app/go-svc 夹具至临时目录，验证生成、真实文件/符号链接关系、重复运行幂等与 `--validate`；另验证双独立文件冲突返回 2 且不覆盖原文。全部通过，原夹具未改。
- Python 语法、模板/追加规则一致性、新 vault 路由通过；未新增永久测试或测试依赖。
- 12 个活动入口检查：9 个通过 quick_validate 原始检查；farm/guide-me/wtf 的既有 `disable-model-invocation` 不在该校验器白名单。保留字段，临时移除该字段后其余结构校验通过；已有 frontmatter 与 agents/openai.yaml 的调用策略逐一比对未变。
- 新增和改动文档的 20 个可解析本地 Markdown 目标存在；模板中的 PROJECT_MEMORY.md 是生成位置的同级文件，已通过临时夹具验证。
- effective-html：checkpoint 与当前版本均运行 14 个测试、23 个失败断言、0 个错误，失败标识逐一相同；改名对应的 `test_skill_activates_overlay_only_for_target_templates` 通过。旧哈希及模板断言失败不在本次修复范围，完整旧套件不是全绿。原定“归档相关现有测试通过”据此收窄为改名测试通过且无新增失败，没有修改旧断言来求绿。
- archdotfiles：原工作区运行因沙箱拒绝创建仓库内临时夹具而失败；在当前 tracked 文件的临时副本中重跑 `make test`，107 项全部通过。测试副本有独立 Git 索引，未操作真实部署目标。
- 两个仓库的本次命名文件 `git diff --check` 通过。未运行视觉回归，CSS、shell、构建器、既有页面均未改。
- 对照日志：`/tmp/skills-audit-checkpoint-20260905/legacy-baseline-comparison.json`、`before-tests.log`、`after-tests.log`、`archdotfiles-tests.log`。全局原文与哈希见同目录 manifest.json。

### 交付状态

本轮计划内修改和检查已完成；改动留在工作区供审阅，未提交或推送。
全局文件经现有符号链接生效，后续会话读取新版本；本会话已加载的指令不被追溯替换。
仓库技能尚未同步到安装副本，已安装的 effective-html 也未删除。模型行为与耗时收益尚未做实验。

## 语义复核（用户选择下一步 1）

范围：复核授权边界、入口与引用的一致性、核心输出契约及归档完整性；发现明确冲突时最小修正。
不提交、不推送、不同步安装副本、不调用其他模型，不改视觉实现或系统配置。
本轮修改前快照：`/tmp/skills-audit-checkpoint-20260905/semantic-review-before/`。
验收：每个发现有处置结论；修改后的指令分支不扩大授权，相关样例与引用检查通过。

- [x] 核对全局边界与全部技能保留的关键契约。
- [x] 修正 arXiv、STORM 引用中的旧执行规则与入口冲突。
- [x] 修正批量归档示例的输出边界，明确 sweep 只读分支。
- [x] 核对页面源码与合成产物的契约差异，完成定点验证。

### 复核结论

复核已完成。本轮修正 5 处指令问题；未发现需要阻止本轮交付的其余语义冲突。
该结论限于静态指令及定点样例验证，不代表安装同步或模型行为验收完成。

| 问题                                                      | 处置与依据                                                                                              |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| arXiv 引用按页数自动缩成导读，与入口尊重用户指定范围冲突  | 移除强制篇幅和缩写表；全文、摘要、导读由用户意图决定。标明代码仅展示截断预览，未读章节不能冒称已总结。  |
| STORM 入口默认顺序研究，引用却默认派代理并硬编码 TinyFish | 引用改为顺序/已授权协作共用协议；搜索使用当前环境可用且允许的工具。保留追问循环、真实来源与结构化发现。 |
| X 批量示例默认当前目录且用覆盖模式打开文件                | 仅明确批量归档时执行，要求选定输出目录，校验文件名来源并独占创建；同名文件直接失败，保留原文。          |
| Obsidian sweep 的只读声明与后续创建步骤仍可能混读         | 在探测布局后显式跳到盘点，不执行创建、查重或写入；无目录时结束。                                        |
| 页面入口把源码占位符当成所有阶段必须存在                  | 对照 build.py 的替换行为，限定占位符属于未合成源码；成品保留内联资源，不重新插入占位符。                |

其余边界核对：

- 全局不可逆操作口令、外部模型逐次确认、用户改动保护仍在；三个全局文件与本轮开始哈希相同，本轮未改系统配置。
- bootstrap 仍保护源文件选择与冲突拒绝，初始化/维护分流未丢失验证、证据或压缩规则。
- book 保留章节 slug/文件映射、来源与保密要求、实际页面验收；不因已授权大纲重复询问。
- farm 仍需独立复验，worker 启动不算任务完成；已有手动调用策略未变。
- guide-me 保留只评估、输出后等待选择；wtf 保留中文重述；makemake 保留真实命令出处及展开验证与实际执行的区别。
- 叙事格式原文完整迁移，HTML 细节仅调整相对模板路径。弃用入口除档案说明外与 checkpoint 原文相同。
- 逐文件比对 checkpoint：叙事资产 11 项、页面资产 43 项与构建脚本 1 项、书籍模板 33 项、旧 HTML 资产 25 项及脚本 4 项均未改变。

### 本轮定点验证

- X 批量归档 Python 样例在临时目录执行：首次写到选定目录；第二次返回 FileExistsError，原文件内容保持不变。没有发起网络请求，没有新增永久测试。
- 现有 `BuildCliTests.test_build_inlines_owned_css_once` 与 `test_build_is_idempotent`：2 项通过。
- 本轮修改的 3 个活动入口 quick_validate 通过；12 个活动技能原有非 description 元数据逐一比对未变，20 个本地引用目标存在。
- `git diff --check` 通过。bootstrap 实现、旧归档测试及全局文件本轮未改，不重复运行其已记录检查；旧套件历史失败仍保留，不计为本轮通过。
- 未提交、未推送、未同步安装副本。下一阶段仍是按范围提交与安装交付；执行前需保留 archdotfiles 中原有修改的归属。

## 本地交付（用户授权继续至完成）

目标：按本次范围提交两个仓库，在本地 main 集成，并同步已安装技能。验收：提交归属准确，原有用户修改保留，安装路径与提交内容一致。
非目标：远程推送、模型效果实验、无关技能升级或清理。

- [ ] 提交 skills 的实施与复核结果。
- [ ] 提交 archdotfiles 的本次增量，原有 Claude 写作段落新增与 Pi 风格章节删除继续留在工作区。
- [ ] 本地 main 快进集成；保留 checkpoint。
- [ ] 备份并同步已有安装副本，核对入口与引用。

安装预检：12 个活动技能均已安装，farm 与 guide-me 直接链接到仓库；其余 10 个副本中，本次目标文件没有独立冲突。仅同步本次改动路径，保留安装器元数据、无关文件及目标特有内容。
