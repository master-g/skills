# {{PROJECT_NAME}} — Claude 项目上下文

> 项目指令与常用命令，供 Claude / AI 编码代理按任务需要查阅。
> 跨会话记忆见 [PROJECT_MEMORY.md](./PROJECT_MEMORY.md)。

## 启动流程

1. 按当前任务需要读取本文件及 PROJECT_MEMORY.md 中相关的决策或交接记录。
2. 需要基线对照时运行相关检查；记录既有失败，不自动修复与本次任务无关的问题。
3. 需要理解近期变更时查看相关 Git 历史。

## 技术栈

<!-- 使用的语言、框架、工具链、版本约束。例: Python 3.12 / FastAPI / uv / Postgres 16 -->

- _待填写_

## 命令

<!-- 常用脚本、构建命令、测试命令、lint/格式化命令。让代理无需猜测即可运行。 -->

- 安装依赖: _待填写_
- 构建: _待填写_
- 测试: _待填写_
- Lint / 格式化: _待填写_

## 代码风格

<!-- 命名约定、格式规则、架构偏好。例: 函数用 snake_case、组件单文件、优先组合而非继承 -->

- _待填写_

## 禁止文件

<!-- 绝对不能修改的文件清单: 生成物、密钥、锁文件、迁移历史、第三方 vendor 目录等 -->

- _待填写_

## 审查规则

<!-- PR 审查的标准和流程: 必须通过的检查、谁审、合并条件、提交信息规范 -->

- _待填写_

## 完成判定

- 已交付本次约定的结果。
- 与本次改动相关的检查有实际结果；修复本次引入的失败并重跑受影响检查。
- 有必要的交接信息时，按项目回写约定记录验证命令、结果和停留位置。
- 明确说明未运行的检查、既有失败及仍影响完成的限制。

## 项目记忆 (回写约定)

跨会话的持久信息记录在 [PROJECT_MEMORY.md](./PROJECT_MEMORY.md)。
**完成每个重要任务后务必回写**,这样下一次会话能接上而不是从零开始:

- 把确认过的决策 / 约束写入「已验证的事实」
- 把踩过的坑、走不通的路径写入「失败尝试」,避免重复
- 用本次进展更新「上次会话」(做了什么、停在何处)
- 把接下来的计划写入「下次运行」

保持 PROJECT_MEMORY.md 在 300~400 行;超长时**保留**「已验证的事实」与「下次运行」,**淘汰**最旧的「失败尝试」「上次会话」条目(用 `scripts/memory.py compact` 自动完成)。
