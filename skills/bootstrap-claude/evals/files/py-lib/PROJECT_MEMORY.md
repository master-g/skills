# 项目记忆 — textkit

> 长度上限约 400 行。超长时保留「已验证的事实」与「下次运行」，淘汰最旧的「失败尝试」「上次会话」。
> 条目用 `- [YYYY-MM-DD]` 前缀，新条目追加在末尾。

## 已验证的事实
- [2026-05-20] slugify 必须保留 Unicode 字母，不能用 ASCII 化方案。

## 失败尝试
- [2026-05-21] 试过用 str.translate 做 slug，对中文无效，放弃。

## 上次会话
- [2026-06-01] 实现了 slugify 的基础版本，停在补单元测试。

## 下次运行
- [2026-06-01] 给 slugify 加 property-based 测试。
