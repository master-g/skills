# X / Twitter 提取

复用会话中已提取的文章。否则从当前技能清单定位并使用 `x-to-markdown`，遵守其支持的链接类型、完整性检查与回退规则。

没有该技能时，使用当前可用的站点提取或浏览器工具，确认得到指定文章而非登录页、卡片摘要或推荐流。
工具失败只尝试有依据的可用回退，不依赖历史会话中某个服务“总是可用”的结论。

保留原始 URL、作者、时间和必要图片出处；线程需核对后续内容，长文需核对结尾。
无法取得全文时明确说明缺失范围，不继续生成声称覆盖全文的文档，也不索取或输出令牌。
