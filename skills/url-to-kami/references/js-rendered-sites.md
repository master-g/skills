# JS 页面提取

普通正文提取返回空白、登录墙或客户端错误时，使用当前环境提供的浏览器工具。
先读取其 API 文档，再导航至用户指定 URL，等待可观察的正文出现。

核对页面标题、正文容器和结尾。`article` 或 `main` 可用于定位，但首个容器不一定覆盖完整线程或长文。
长文分段读取，保留顺序；输出截断仅是当前查看窗口，不能把截断文本当完整提取。

仅使用工具允许的 DOM 读取能力。不启动假定存在的 browser-harness，不杀死用户浏览器或重新配置其调试端口。
浏览器不可用时，可以检查直接请求得到的 HTML 是否已包含正文；没有完整材料就报告缺口。
